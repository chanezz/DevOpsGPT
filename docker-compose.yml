version: '3.9'

services:
  chat-api:
    build: ./backend
    ports:
      - "3000:3000"

  chat-ui:
    build: ./frontend
    ports:
      - "5173:5173"

  cache:
    image: redis:7-alpine
    ports:
      - "6379:6379"